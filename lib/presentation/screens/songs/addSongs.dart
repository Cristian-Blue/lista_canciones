import 'package:flutter/material.dart';

class AddSongs extends StatelessWidget {
  const AddSongs({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Add songs");
  }
}
