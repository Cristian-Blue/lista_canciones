import 'package:flutter/material.dart';

class ListSongs extends StatelessWidget {
  const ListSongs({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("List Songs");
  }
}
